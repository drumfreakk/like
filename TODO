TODO: instead of using ip to check wether you have voted for(then you can only vote once from an ip),
use an external (login) cookie from, say, discord to see wether you have voted before,
and if you are not logged in to discord it will redirect you to a page to log in to discord
